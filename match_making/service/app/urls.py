
from django.urls import path, include


from . import views

urlpatterns = [
    # path("put_in_queue/", views.put_in_queue.as_view(),),
    # path("clear/", views.clear_queue),
    # path("in_queue/", views.in_queue.as_view()),
    # path("quit_queue/", views.quit_queue.as_view()),
    # path("get_view/", views.get_view),
    # path("", views.get_queue)
]
